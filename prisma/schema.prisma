/// Prisma Client generation
generator client {
  provider     = "prisma-client"
  output       = "../generated/prisma"
  moduleFormat = "cjs"
}

/// Primary PostgreSQL datasource
datasource db {
  provider = "postgresql"
}

/// Domain models will be added below in future tickets.
